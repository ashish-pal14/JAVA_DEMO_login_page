# Java Login App Without Database

This is a simple Spring Boot application with a frontend login page and backend validation.

## Default credentials

- Username: `admin`
- Password: `admin123`

## Run

```bash
mvn spring-boot:run
```

Open `http://localhost:8080/login` in the browser.